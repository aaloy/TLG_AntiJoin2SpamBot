——————————————————
Ajuda sobre el Bot
——————————————————

* Para hacer funcionar el Anti*Spam, es necesario añadirme a un grupo y otorgarme privilegios de administración para que pueda leer mensajes y borrar aquellos que sean Spam.

* Una vez con privilegios de administración, llevaré un control sobre los usuarios que se unan al grupo y evitaré que escriban mensajes que contengan URLs hasta que haya pasado un tiempo específico desde que se unieron, y hayan escrito el suficiente número de mensajes.

* El tiempo que deben esperar los nuevos usuarios y el número de mensajes que deben de escribir antes de poder publicar mensajes con URLs son, por defecto, {allow_urls} horas y {min_msg} mensajes, pero estos valores pueden modificarse y configurarse haciendo uso de los comandos /set_messages y /set_hours.

* También detecto usuarios con nombres largos y/o que presentan URLs en sus nombres, cuando estos se unen a un chat, aviso al respecto y elimino el mensaje de Telegram de que dicho usuario se unió al chat.

* Para mantener limpio el grupo, elimino aquellos mensajes que tengan relación conmigo, pasados {autodestruct} minutos (salvo mensajes relacionados con la detección de Spam y llamada a Administradores).

* Los comandos de configuraciones y activación/desactivación solo pueden ser utilizados por los Administradores del grupo.

* Puedes configurar el idioma en el que hablo mediante el comando /language.

* Echa un vistazo al comando /commands para ver una lista con todos los comandos disponibles y una breve descripción de cada uno de ellos.

* NOTA: No baneo usuarios, solo detecto, elimino y notifico sobre mensajes de Spam (Esta es la filosofía del desarrollador), el Bot notifica sobre el Spam, pero es un
humano quien debe decidir si banear a ese usuario o no.,
